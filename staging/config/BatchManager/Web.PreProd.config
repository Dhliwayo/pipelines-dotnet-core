<?xml version="1.0"?>
<!-- This is an example transform file for PreProd environment -->
<!-- It demonstrates how to transform connectionStrings and appSettings -->
<configuration xmlns:xdt="http://schemas.microsoft.com/XML-Document-Transform">
  
  <!-- Transform connectionStrings -->
  <connectionStrings>
    <!-- Replace the entire connectionString attribute for a specific named connection -->
    <add name="ESRIUK.xxxxxxxxx.PerilsAndProximity.Batch.Properties.Settings.tttConnectionString" 
         connectionString="Data Source=#{DB_SERVER}#;Initial Catalog=#{DB_NAME}#;Persist Security Info=True;User ID=#{DB_USER}#;Password=#{DB_PASSWORD}#" 
         xdt:Transform="SetAttributes" 
         xdt:Locator="Match(name)"/>
    
    <add name="BatchDataAccess.Properties.Settings.tttConnectionString1" 
         connectionString="Data Source=#{DB_SERVER}#;Initial Catalog=#{DB_NAME}#;Persist Security Info=True;User ID=#{DB_USER}#;Password=#{DB_PASSWORD}#" 
         xdt:Transform="SetAttributes" 
         xdt:Locator="Match(name)"/>
  </connectionStrings>

  <!-- Transform appSettings -->
  <appSettings>
    <!-- Keep the ActiveConnectionString as is - it references the correct connection -->
    <!-- No need to transform this since it already points to the right connection string name -->
    
    <!-- Add environment-specific setting -->
    <add key="Environment" 
         value="PreProduction" 
         xdt:Transform="InsertIfMissing" 
         xdt:Locator="Match(key)"/>
    
    <!-- Add API endpoint that might be different per environment -->
    <add key="ApiEndpoint" 
         value="https://preprod-api.example.com" 
         xdt:Transform="InsertIfMissing" 
         xdt:Locator="Match(key)"/>
    
    <!-- Add database timeout for PreProd -->
    <add key="DatabaseTimeout" 
         value="30" 
         xdt:Transform="InsertIfMissing" 
         xdt:Locator="Match(key)"/>
  </appSettings>

  <!-- Keep debug on for PreProd -->
  <system.web>
    <compilation debug="true" xdt:Transform="SetAttributes(debug)"/>
  </system.web>

  <!-- Remove specific WCF endpoint in PreProd -->
  <system.serviceModel>
    <services>
      <service>
        <endpoint address="mex" binding="mexHttpsBinding" contract="IMetadataExchange" xdt:Transform="Remove" xdt:Locator="Match(address, binding, contract)"/>
      </service>
    </services>
    <bindings>
      <webHttpBinding>
        <binding name="webBindingHTTPS" xdt:Transform="Remove" xdt:Locator="Match(name)"/>
      </webHttpBinding>
    </bindings>
  </system.serviceModel>

  <!-- Adjust log level for PreProd -->
  <log4net>
    <root>
      <level value="INFO" xdt:Transform="SetAttributes(value)" xdt:Locator="Match(value)"/>
    </root>
  </log4net>

</configuration>

